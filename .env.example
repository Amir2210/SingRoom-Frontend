# Audius API key (free) — create one at https://api.audius.co/plans
# Only the API Key is needed (safe for client-side use). Do NOT put the Bearer Token here.
VITE_AUDIUS_API_KEY=your_audius_api_key_here
